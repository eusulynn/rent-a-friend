<div class="class">
<h1>All Friends</h1>

  <ul>
    <% @friends.each do |friend| %>
      <li><%= link_to friend.name, friend_path(friend) %> </li>
    <% end %>
  </ul>
<%= link_to "List me as a friend!",  new_friend_path, class: "btn btn-primary mb-2" %>
</div>
